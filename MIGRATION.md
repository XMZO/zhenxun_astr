# 真寻签到迁移记录

## 迁移范围

| 真寻原版内容 | AstrBot 版本处理 |
| --- | --- |
| `签到` | 保留，按时区每日判重并发送原版布局卡片 |
| `我的签到` | 保留，发送原版“我的信息”卡片区 |
| 重复签到判定 | 保留，同一用户同一自然日只记录一次 |
| 累计签到次数 | 保留 |
| 签到图片 | 使用官方资源仓库的默认签到模板和素材 |
| 好感度数值 | 不迁移；原版底部位置保留静态占位区 |
| 好感度排行/签到排行 | 移除指令 |
| 好感度双倍卡 | 不迁移 |
| 金币 | 字段和奖励入口预留，默认关闭 |
| 道具 | 库存字段和奖励入口预留，默认关闭 |
| 商店与道具效果 | 暂缓 |
| `astrbot_plugin_Favour_Ultra` 兼容 | 暂缓，未来作为独立扩展模块接入 |

## 模板与素材

真寻源码在 `zhenxun/builtin_plugins/sign_in/utils.py` 中组装 `card_data`，随后调用：

```text
ui.render_template("pages/builtin/sign", data=card_data)
```

渲染器会读取主题目录中的：

```text
themes/default/pages/builtin/sign/main.html
themes/default/pages/builtin/sign/style.css
themes/default/pages/builtin/sign/manifest.json
```

本插件从资源仓库提交 `08ae663cb7fabfc64c11ddad2489b55fa86cf96f` 提取同一组文件。`sign_card.css` 与官方 CSS 字节一致；`sign_card.html` 保留官方卡片 DOM 和 CSS 类，只将资源函数和业务字段改成 AstrBot 数据变量。

原版卡片中的“真寻”不是写在人物图片里的文字：默认名称来自真寻配置并参与 Python 生成的问候语，其他标签则写在 HTML 模板中。本插件把这些内容归入模板设置，人物图和图标仍是模板素材，可以直接替换。

## 无好感度实现的处理

原版模板底部有当前好感度、等级、态度、升级距离、心形和进度条。为了不改变原版尺寸和视觉结构，本插件保留这些 DOM 位置，但默认使用：

- 当前值：`--`
- 等级：`未接入`
- 态度：`对你的态度: 未接入`
- 升级距离：`距离升级还差--好感度`
- 空心心形 8 个，进度 0%

这些值不是好感度逻辑，也不会写入签到存储。未来扩展模块只需要提供卡片数据，不必把兼容代码写死在基础签到流程中。

## 日期问题处理

原版当前源码通过签到日志的时间转换后比较日期，没有固定的 2025/2026 年份上限。若旧部署在某个年份后失效，更可能是旧数据库结构、迁移脚本或图片缓存问题。

本插件不复用真寻旧日期表：

- 每个用户只保存 `last_sign_date`，格式为 ISO 日期字符串。
- 判重只比较当前配置时区的日期。
- 日期解析兼容 ISO 日期、ISO datetime 和常见旧格式，不限制年份。
- 写入采用原子替换；损坏数据会先隔离为 `.corrupt-...json`。

跨年行为由 `tests/test_storage.py` 覆盖：`2025-12-31` 签到后，`2026-01-01` 可以再次签到。

## AstrBot 适配细节

官方模板使用真寻渲染器的 `asset()` 和本地 `file://` 资源。AstrBot 远程 T2I 端点无法读取这些路径，因此插件在本地把官方字体和选中的 PNG 编码为 `data:` URI，再提交完整 HTML。远程端点默认输出宽度大于原版，插件最后裁切到 `465×926` 并转换为 PNG，避免改变卡片比例或被消息平台按 `.jpg` 扩展名误判。

## 模板包扩展

根目录中的官方模板继续作为默认模板运行。自定义模板采用自包含目录或 ZIP，入口由 `template.json` 描述；插件同时扫描插件内 `template_packs` 与数据目录的 `template_packs`，数据目录中同内部 ID 的包优先。

编辑器为新模板生成随机内部 ID，AstrBot 设置页的“当前模板”下拉框只显示模板名称；同名模板用自动编号区分。当前选择保存在插件配置中，不再提供聊天切换指令。

模板包拥有自己的文字、HTML、CSS、字体与图片。内置模板从根目录 `template_settings.json` 读取内容，自定义模板从各自 `template.json.settings` 读取；插件配置只保留模板安装与选择、指令前缀、时区、UID 隐私和奖励等运行级字段。可视化编辑器直接生成并安装模板包，不再要求把生成配置合并到现有 AstrBot 配置。

AstrBot 4.27 的原生 `file` 配置控件用于设置页 ZIP 上传。上传路径由 AstrBot 限制在本插件的 `plugin_data` 文件区；插件再次检查相对路径和扩展名，完整验证 ZIP 后才通过临时文件原子安装到持久 `template_packs`。同一内容重复保存是幂等操作，同一内部 ID 的新内容会更新安装副本，失败包不会替换旧包。上传源与安装副本分离，删除上传源不会隐式卸载模板。
